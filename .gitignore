# raw browser-saved pages (1.2 GB) — parsed into program-details/ + residency_explorer.db
data/

# generated locally: python3 load_programs.py && python3 load_links.py && python3 load_details.py
# (the copy in web/ IS committed — Vercel serves from it)
/residency_explorer.db

# next.js
web/node_modules/
web/.next/
web/out/

.DS_Store
*.log
